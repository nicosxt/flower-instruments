using System.Collections;
using System.Collections.Generic;
using UnityEngine;
public class EA_Instrument_Drum : EA_Instrument
{
    //require gameObject to have AudioSource

    public EA_NoteObject[] drums;
    // Start is called before the first frame update
    void Start()
    {
        base.Start();
    }

    // Update is called once per frame
    void Update()
    {
        base.Update();
    }


}
